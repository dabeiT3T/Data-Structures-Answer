#!/usr/bin/env python3
'''
Author: dabei
My answer to Chapter 4 Quiz 7.
'''

'''
# class Grid in book
from grids import Grid

class Matrix(Grid):

    def __add__(self, other):
        # same type
        # same size
        # add the items
        tmp = [[0] * self.getWidth()] * self.getHeight()
        for i in range(self.getHeight()):
            for j in range(self.getWidth()):
                tmp[i][j] = self[i][j] + other[i][j]
        return tmp
'''
